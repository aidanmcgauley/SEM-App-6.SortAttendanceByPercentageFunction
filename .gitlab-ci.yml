image: node:14-alpine

stages:
    - test

test:
    stage: test
    script:
        - npm install
        - npm test

# Testing to see if the cache speeds up subsequent builds
cache:
    paths:
        - node_modules/