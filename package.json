{
    "scripts": {
        "test": "jest",
        "deploy": "gcloud functions deploy sortbypercentage --runtime nodejs14 --trigger-http --region us-east1"
    },
    "devDependencies": {
        "jest": "^27.0.0"
    },
    "dependencies": {
        "@google-cloud/functions-framework": "^3.0.0"
    }
}